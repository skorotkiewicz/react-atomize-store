import React from "react";
import { render } from "@testing-library/react";
import { useStore, useAtom } from "./Atomize";

describe("Atomize", () => {
  test("render component", () => {
    render(<></>);
  });
});
